query DailyDistanceReport(
    $IMEINumber: String!
    $startDateTime: String!
    $endDateTime: String!
) {
    dailyReport(
        IMEINumber:$IMEINumber,
        startDateTime:$startDateTime,
        endDateTime:$endDateTime
    )
}